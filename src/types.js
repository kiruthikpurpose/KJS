/**
 * @typedef {Object} KJSState
 * @property {Object} [initialState] - The initial state of the application.
 * @property {Array<Object>} [routes] - The routes configuration for the application.
 */

/**
 * @typedef {Object} Component
 * @property {Function} render - Renders the component and returns HTML string.
 * @property {Function} mount - Mounts the component into the specified container.
 */

/**
 * @typedef {Object} Store
 * @property {Object} state - The current state of the store.
 * @property {Function} setState - Updates the state of the store.
 */
